# ThreatFusion
A web app that takes a URL or file hash (MD5/SHA-1/SHA-256) and tells you whether it's safe, suspicious, or malicious by querying multiple threat intelligence APIs and combining their results into a single risk score (0–100).
